Um die Google Earth API anzusprechen muss die HTML 
mit dem Absoluten Pfad angegeben werden. 

Das File liegt in dem Ordner:

"EarthExplorer\googleEarthComponent\index2.hmtl"

Der komplette Pfad muss dann in der 

"MainWindow.xaml.cs" angegeben werden. 

Suche nach dem Begriff "GoogleHTML" für die entsprechende Stelle

Viel Spaß damit.